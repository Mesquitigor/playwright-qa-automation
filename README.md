## Dummy Json - Products

This project contains automated API tests for the product 'Products' using Playwright with Typescript.

## Tool viability analysis

Compare Playwright vs lighter runners (time and RAM):

```bash
npm run analyze:tools
npm run analyze:tools:benchmark
```

Reports are written to `reports/tool-viability-report.json` and `reports/tool-viability-report.md`.
